-- 每一版的变更说明。**选填。**
--
-- 在此之前版本列表只有「v3 · 2026-08-21 10:59 · alice」——「这一版改了什么」
-- 完全答不上来，而这恰恰是回滚前唯一想知道的事：要切回去，先得知道切掉的是什么。
-- 靠 diff 两份 definition 也答不了：JSON 差异说得出"某个节点的 sql 变了"，
-- 说不出"改成按天分区，之前扫全表超时"。
--
-- 选填而不是必填：发布本来就该是低摩擦的动作，加一道必填的输入框只会让人
-- 随手敲个「更新」——那比空着更糟，空着至少是诚实的。
ALTER TABLE flow_versions ADD COLUMN IF NOT EXISTS note TEXT;
