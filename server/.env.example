# 复制成 server/.env 再填。.env 已在 .gitignore 里，不要提交。

# ---------- 机器人账号（必填） ----------
# 所有查询都用它换 accessToken（grant_type=password）。票 2 小时有效，
# 服务端提前 2 分钟自动续，流程感知不到。
#
# 注意：机器人名（如 cs_help_robot）是 USERNAME，不是 CLIENT_ID —— 这是两对
# 不同的凭证，弄混会被拒成 "Invalid client: client is invalid"。
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_USERNAME=
OAUTH_PASSWORD=

# 一般不用改
OAUTH_TOKEN_URL=https://oauth.agoralab.co/oauth/token

# ---------- 数据平台 ----------
# 默认 LA3，不用配。切 TJ3 才打开下面这行：
# DATALEGO_ENDPOINT=https://datalego.tj3d.agoralab.co/api/v1

# ---------- 身份与隔离 ----------
# 谁在用、以谁的权限查数，由服务端把浏览器 Cookie 转发到固定的
# https://athena.agoralab.co/api/me 验证；AutoFlow 不解析 HCIAuthToken。
# 不需要额外配置，但生产部署必须让浏览器 Cookie 能到达本服务。
# 自检：GET /whoami，它会直说这次用的是谁、从哪来的。

# worker 代跑定时/webhook 任务时没有 cookie，身份取"这一版是谁发布的"，
# 通过请求头带过来。**这个头只有密钥对得上才认**，没配就一律不认（fail closed，
# 结果是定时任务退回机器人权限，而不是敞开让人冒充）。
# worker 侧配同一个值：WORKER_TOKEN=...
# 生成：openssl rand -hex 32
# WORKER_TOKEN=

# ---------- 自建 PostgreSQL 工作区 ----------
# 工作区与流程控制库分离。先由 PostgreSQL 管理员运行：
# WORKSPACE_ADMIN_PASSWORD=... bash deploy/init-workspace-db.sh
# WORKSPACE_ADMIN_DSN=postgresql://autoflow_workspace_admin:密码@127.0.0.1:5432/autoflow_workspace
# WORKSPACE_ROLE_SECRET=
# WORKSPACE_QUOTA_BYTES=1073741824

# ---------- http.request 出网白名单 ----------
# 不配的话：公网随便打，内网/回环/云元数据一律拒（防的是"谁都能打开编辑器，
# 让服务端去读 169.254.169.254 再把响应体贴回画布"）。
#
# 一旦配了就变成**严格白名单**：只有列出的主机能出网，公网也一起被拦。所以为了
# 放行一个内网地址而配这行时，**必须把现有流程用到的公网主机一并列上**，否则它们
# 会当场报"不在出网白名单里"。只管主机名，端口和路径不看。
#
# 不要加引号——.env 解析器不剥引号，加了会连引号一起当成主机名，然后什么都匹配不上。
# 只影响 http.request 节点；企微通知走 wecom.py，不经过这道校验。
# 如果只需放行一个内网端点，优先使用精确 URL 例外；它不会拦掉其他公网主机。
# HTTP_NODE_ALLOWED_URLS=http://127.0.0.1:7789/gw/send
# HTTP_NODE_ALLOWED_HOSTS=127.0.0.1 athena.agoralab.co

# 在默认拦截网段之外再追加，例如 k8s 用了 100.64.0.0/10。只能加不能减。
# HTTP_NODE_BLOCKED_CIDRS=100.64.0.0/10

# ---------- 开发 ----------
# 本地假身份。**只在本地开发用，生产绝不要配。**
#
# 本机没有 Athena 的登录 cookie，认得出用户才出现的界面（首页的「失败通知」、
# 按人隔离的流程列表、管理台）一个都看不见。配上这一行就能看见了。
#
# 三道闸全开才生效，缺一不认：① 这一行写了合法邮箱；② PGHOST 没设（生产容器
# 一律走 PG*，那是生产形态）；③ 这次请求没带 cookie（有人在真登录时以真的为准）。
# 开着的时候服务启动会打一条醒目警告，/whoami 的 source 报 "dev" 而不是 "athena"。
# DEV_IDENTITY_EMAIL=you@agora.io
# 顺带把自己变成管理员（能看用量看板和全部用户的流程）
# DEV_IDENTITY_ADMIN=1

# ---------- Python 代码节点 ----------
# 默认关闭：下面两条路都没配时，code.python 节点直接报「沙箱未配置」——
# 生产不会因为"忘了配"而多出一个代码执行口。
#
# 本地开发子进程模式：环境变量清空、独立 venv、超时 SIGKILL，但**没有**
# 文件系统/内存隔离（真正的隔离是未来的沙箱容器）。用户代码可以联网 ——
# 这是有意的决策，代价见 code_python.py 开头。
# 和 DEV_IDENTITY 同款闸门：PGHOST 存在（生产形态）时这个开关一律不认。
# CODE_NODE_LOCAL_EXEC=1
#
# 沙箱解释器路径。默认 server/.venv-sandbox/bin/python（scripts/dev.sh 自动建，
# 预装包由管理员在「Python 依赖」页控制）。
# SANDBOX_PYTHON=
#
# 沙箱服务地址。配了就 HTTP 转发过去，优先于本地模式（协议见
# code_python._forward）。Docker 部署由 compose 自动配；裸机服务器用
# scripts/sandbox.sh 起沙箱进程后配下面这行并重启 api：
# SANDBOX_URL=http://127.0.0.1:9000
#
# 给沙箱 venv 装包用的 pip 源（公司镜像）。不配走默认源。
# PIP_INDEX_URL=

# 前端 dev server 的地址，多个用空格分隔。正式部署走网关同源转发时留空。
CORS_ORIGINS=http://localhost:5273

# 服务监听端口。默认 8791 —— 别用 8787，那个被内网的 agora-gateway 占着。
PORT=8791
