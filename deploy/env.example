# 复制成 deploy/app.env 再填。**绝不提交进 git**（.gitignore 已经挡了）
#
# 数据平台的机器人账号。凭证只有这一个来源 —— 绝不接受调用方传入，
# 那样会进流程定义、进日志、进 git
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_USERNAME=
OAUTH_PASSWORD=

# worker 以"这一版的发布者"的名义调数据平台时，用它证明自己是 worker。
# api 和 worker 两个容器必须配成同一个值；不配的话定时任务一律退回机器人账号
# 的权限（fail closed —— 宁可少一层隔离，也不留一个加个头就能冒充的口子）。
# 生成：openssl rand -hex 32
WORKER_TOKEN=

# 失败告警里「运行详情」链接的前缀（前端的对外地址，不带路径）。
# 配了就是点开直接看运行记录的地址；不配告警里给的是 /api/runs/{id} 这个 JSON 接口
# PUBLIC_APP_URL=https://autoflow.corp

# 运行日志（每次运行 + 每个节点的输入输出）在库里保留几天，到期由 worker
# 自动清理。默认 14 天；只删已结束的运行，在跑/排队的不受影响
# RUN_RETENTION_DAYS=14

# ---------- 自建 PostgreSQL 工作区 ----------
# 用户建表/增删改查使用独立的 autoflow_workspace 数据库；它与保存流程的
# autoflow 控制库完全分开。初始化一次：
# WORKSPACE_ADMIN_PASSWORD=... bash deploy/init-workspace-db.sh
# 然后填入连接串和稳定随机密钥（生成：openssl rand -hex 32）。
# WORKSPACE_ADMIN_DSN=postgresql://autoflow_workspace_admin:密码@127.0.0.1:5432/autoflow_workspace
# WORKSPACE_ROLE_SECRET=
# WORKSPACE_QUOTA_BYTES=1073741824

# 精确放行单个内网端点（不影响默认的公网放行策略）
# HTTP_NODE_ALLOWED_URLS=http://127.0.0.1:7789/gw/send
# http.request 节点的严格主机白名单（留空 = 拦内网、放行公网）
# HTTP_NODE_ALLOWED_HOSTS=qyapi.weixin.qq.com internal.corp
# k8s 用了 100.64/10 之类的话在这里追加
# HTTP_NODE_BLOCKED_CIDRS=100.64.0.0/10

# ---------- Python 代码节点 ----------
# SANDBOX_URL 已由 docker-compose 直接配给 api（http://sandbox:9000），
# 这里不用管。没配 SANDBOX_URL 的部署里 code.python 节点报「沙箱未配置」
# 拒绝执行（默认闸死，不留洞）。本地开发的子进程模式（CODE_NODE_LOCAL_EXEC）
# 在生产容器里一律不认：按「PGHOST 存在 = 生产形态」判定，见 server/.env.example。
#
# 沙箱容器里 pip 装包要走公司源时，把源地址配给 sandbox 服务（compose 里加
# environment: PIP_INDEX_URL=...）；沙箱容器绝不挂这份 app.env —— 里面全是凭证。
